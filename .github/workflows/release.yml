name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write
  packages: write

env:
  IMAGE: ghcr.io/${{ github.repository }}

jobs:
  image:
    name: Образ страницы подписки
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      # Dockerfile кладёт собранный интерфейс внутрь образа, поэтому его
      # нужно собрать до сборки образа.
      - name: Сборка интерфейса
        working-directory: frontend
        run: |
          npm ci --include=dev
          npm run cb

      - name: Buildx
        uses: docker/setup-buildx-action@v3

      - name: QEMU
        uses: docker/setup-qemu-action@v3

      - name: Вход в GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Версия
        run: echo "VERSION=$(jq -r .version backend/package.json)" >> "$GITHUB_ENV"

      - name: Сборка и отправка образа
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ env.VERSION }}
            ${{ env.IMAGE }}:latest

      - name: Релиз
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          name: XPANEL Subscription Page ${{ env.VERSION }}
